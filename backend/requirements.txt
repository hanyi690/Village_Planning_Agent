# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# File parsing - MarkItDown (Microsoft)
# 支持 docx, pdf, xlsx, pptx 等多种格式转换为 Markdown
markitdown[docx,pdf,xlsx,pptx]

# Encoding detection
chardet>=5.0.0

# File type detection
filetype>=1.2.0

# Timeout protection (prevent large file hangs)
func-timeout>=4.3.5

# CORS
fastapi-cors>=0.0.6

# Database
sqlmodel>=0.0.14
aiosqlite>=0.19.0

# LangGraph checkpoint
langgraph-checkpoint-sqlite>=3.0.3
