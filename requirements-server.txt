# ============================================
# Server-Only Requirements (阿里云 Embedding 模式)
# ============================================
# 此文件用于生产环境部署，移除了本地 ML 模型依赖
# 使用阿里云 DashScope Embedding API，无需本地模型
# 镜像大小: ~1-2GB（相比完整版减少 6-7GB）

# ============================================
# LangChain Ecosystem (v1.x)
# ============================================
langchain>=1.1.0
langchain-core>=1.2.0
langchain-community>=0.4.1
langchain-openai>=1.1.0
langchain-text-splitters>=1.0.0
langchain-chroma>=1.0.0
# 注意: 移除了 langchain-huggingface（本地模型依赖）

# ============================================
# LangGraph (v1.x)
# ============================================
langgraph>=1.0.0
langgraph-checkpoint>=4.0.0
langgraph-checkpoint-sqlite>=3.0.0
langgraph-prebuilt>=1.0.0
langgraph-sdk>=0.3.0

# ============================================
# LLM Providers
# ============================================
openai>=2.0.0
zhipuai>=2.1.4

# ============================================
# Vector Database (精简版)
# ============================================
# chromadb 会安装一些 ML 依赖，但比 sentence-transformers 小很多
chromadb>=1.0.0

# ============================================
# Web Framework (FastAPI)
# ============================================
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
fastapi-cors>=0.0.6

# ============================================
# Database
# ============================================
sqlmodel>=0.0.14
aiosqlite>=0.19.0

# ============================================
# File Processing
# ============================================
markitdown[docx,pdf,xlsx,pptx]
filetype>=1.2.0
chardet>=5.0.0
func-timeout>=4.3.5  # 超时保护，防止大文件处理卡死

# ============================================
# Terminal Output
# ============================================
rich>=13.0.0

# ============================================
# Core Dependencies
# ============================================
python-dotenv>=1.0.0
pydantic>=2.12.0
pydantic-settings>=2.0.0
typing-extensions>=4.12.0

# ============================================
# LangSmith Tracing
# ============================================
langsmith>=0.1.0

# ============================================
# GIS Dependencies (空间分析)
# ============================================
geopandas>=0.14.0
shapely>=2.0.0
networkx>=3.0

# ============================================
# 注意: 移除的依赖（本地 Embedding 模式专用）
# ============================================
# sentence-transformers>=2.2.0  → PyTorch (~2GB)
# langchain-huggingface>=0.1.0 → transformers (~1GB)
#
# 如需本地 Embedding 模式，请使用 requirements.txt 完整版
