# qwen3.5 多模态 + 网络搜索功能集成计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 为 Village Planning Agent 集成 qwen3.5 多模态大模型和网络搜索功能，支持 Agent 自主联网获取信息。

**Architecture:**
- ~~Phase 1: 扩展现有 OpenAI provider 支持阿里云百炼 API（qwen3.5），零新增依赖~~ (已跳过)
- Phase 2: 实现轻量级网络搜索工具，可选 Tavily API 作为搜索后端 ✅ 已完成
- Phase 3: 可选 GIS 功能扩展（按需）

**Tech Stack:**
- 复用现有 `langchain-openai` 和 `openai` 包
- 阿里云百炼 OpenAI 兼容接口
- 可选：`tavily-python` 或 `requests` 用于网络搜索

---

## 实现状态

### Phase 2: 网络搜索功能 ✅ 已完成

| 任务 | 文件 | 状态 |
|------|------|------|
| 创建搜索工具 | `src/tools/search_tool.py` | ✅ 已完成 |
| 注册 web_search 工具 | `src/tools/registry.py` | ✅ 已完成 |
| 添加 API 配置 | `.env` | ✅ 已完成 |
| 测试 | `tests/test_search_tool.py` | ✅ 7 测试通过 |
| 使用文档 | `docs/web-search-usage.md` | ✅ 已完成 |

---

## 现状分析

### 已有基础

| 组件 | 文件 | 状态 |
|------|------|------|
| LLM 工厂 | `src/core/llm_factory.py` | ✅ 支持 OpenAI/ZhipuAI |
| 配置中心 | `src/core/config.py` | ✅ 基础配置完整 |
| 工具注册 | `src/tools/registry.py` | ✅ 支持工具注册 |
| RAG 工具 | `src/tools/knowledge_tool.py` | ✅ 已有 knowledge_search |

### .env 现有配置

```
OPENAI_API_KEY=sk-xxx (阿里云百炼 key)
OPENAI_API_BASE=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat
DASHSCOPE_API_KEY=sk-xxx (备用)
```

---

## Phase 1: qwen3.5 集成（零新增依赖）

### Task 1: 扩展 config.py 添加 DashScope 配置

**Files:**
- Modify: `src/core/config.py:1-50`
- Test: 无（配置加载验证）

**Step 1: 添加 DashScope 配置变量**

在 `src/core/config.py` 第 8 行后添加：

```python
# DashScope (阿里云百炼) API 配置
DASHSCOPE_API_KEY = os.getenv("DASHSCOPE_API_KEY")
DASHSCOPE_API_BASE = os.getenv("DASHSCOPE_API_BASE", "https://dashscope.aliyuncs.com/compatible-mode/v1")

# LLM Provider 扩展支持
# LLM_PROVIDER 可选值："deepseek", "openai", "zhipuai", "dashscope"
```

**Step 2: 更新 LLM_PROVIDER 注释**

将第 11 行注释从：
```python
# LLM_PROVIDER: 必填，可选值："deepseek", "openai", "zhipuai"
```

改为：
```python
# LLM_PROVIDER: 必填，可选值："deepseek", "openai", "zhipuai", "dashscope"
```

**Step 3: 验证配置加载**

```bash
python -c "from src.core.config import DASHSCOPE_API_KEY, DASHSCOPE_API_BASE; print(f'DashScope configured: {DASHSCOPE_API_KEY is not None}')"
```

Expected: `DashScope configured: True`

---

### Task 2: 扩展 LLMProvider 枚举支持 DashScope

**Files:**
- Modify: `src/core/llm_factory.py:18-25`

**Step 1: 添加 DASHSCOPE 枚举值**

将第 18-22 行：
```python
class LLMProvider(Enum):
    """LLM Provider enumeration"""
    OPENAI = "openai"
    ZHIPUAI = "zhipuai"
```

改为：
```python
class LLMProvider(Enum):
    """LLM Provider enumeration"""
    OPENAI = "openai"
    ZHIPUAI = "zhipuai"
    DASHSCOPE = "dashscope"
```

**Step 2: 验证枚举扩展**

```bash
python -c "from src.core.llm_factory import LLMProvider; print([p.value for p in LLMProvider])"
```

Expected: `['openai', 'zhipuai', 'dashscope']`

---

### Task 3: 扩展 get_provider 函数支持 dashscope

**Files:**
- Modify: `src/core/llm_factory.py:24-53`

**Step 1: 更新 get_provider 函数逻辑**

将第 44-52 行：
```python
    # Map provider names
    if provider_name in ("deepseek", "openai"):
        return LLMProvider.OPENAI
    elif provider_name == "zhipuai":
        return LLMProvider.ZHIPUAI
    else:
        # Fallback to OpenAI for unknown providers
        logger.warning(f"Unknown provider '{provider_name}', falling back to OpenAI")
        return LLMProvider.OPENAI
```

改为：
```python
    # Map provider names
    if provider_name == "dashscope":
        return LLMProvider.DASHSCOPE
    elif provider_name in ("deepseek", "openai"):
        return LLMProvider.OPENAI
    elif provider_name == "zhipuai":
        return LLMProvider.ZHIPUAI
    else:
        # Fallback to OpenAI for unknown providers
        logger.warning(f"Unknown provider '{provider_name}', falling back to OpenAI")
        return LLMProvider.OPENAI
```

**Step 2: 验证 provider 路由**

```bash
python -c "from src.core.llm_factory import get_provider, LLMProvider; print(get_provider('dashscope') == LLMProvider.DASHSCOPE)"
```

Expected: `True`

---

### Task 4: 添加 get_api_key 支持 DashScope

**Files:**
- Modify: `src/core/llm_factory.py:55-84`

**Step 1: 更新 get_api_key 函数**

将第 68-83 行：
```python
    if provider == LLMProvider.ZHIPUAI:
        key = os.getenv("ZHIPUAI_API_KEY")
        if not key:
            raise ValueError(
                "ZHIPUAI_API_KEY not found in environment. "
                "Please set it in your .env file or environment variables."
            )
        return key
    else:  # OPENAI
        key = os.getenv("OPENAI_API_KEY")
        if not key:
            raise ValueError(
                "OPENAI_API_KEY not found in environment. "
                "Please set it in your .env file or environment variables."
            )
        return key
```

改为：
```python
    if provider == LLMProvider.DASHSCOPE:
        key = os.getenv("DASHSCOPE_API_KEY")
        if not key:
            raise ValueError(
                "DASHSCOPE_API_KEY not found in environment. "
                "Please set it in your .env file or environment variables."
            )
        return key
    elif provider == LLMProvider.ZHIPUAI:
        key = os.getenv("ZHIPUAI_API_KEY")
        if not key:
            raise ValueError(
                "ZHIPUAI_API_KEY not found in environment. "
                "Please set it in your .env file or environment variables."
            )
        return key
    else:  # OPENAI
        key = os.getenv("OPENAI_API_KEY")
        if not key:
            raise ValueError(
                "OPENAI_API_KEY not found in environment. "
                "Please set it in your .env file or environment variables."
            )
        return key
```

**Step 2: 验证 API Key 获取**

```bash
python -c "from src.core.llm_factory import get_api_key, LLMProvider; print(f'DashScope key exists: {get_api_key(LLMProvider.DASHSCOPE) is not None}')"
```

Expected: `DashScope key exists: True`

---

### Task 5: 创建 _create_dashscope_llm 函数

**Files:**
- Modify: `src/core/llm_factory.py:228-230` (在 _create_zhipuai_llm 之后)

**Step 1: 添加 DashScope LLM 创建函数**

在第 228 行后（_create_zhipuai_llm 函数结束后）添加：

```python
def _create_dashscope_llm(
    model: str,
    temperature: float = 0.7,
    max_tokens: int = 2000,
    callbacks: Optional[List[Any]] = None,
    metadata: Optional[Dict[str, Any]] = None,
    streaming: bool = False,
    **kwargs
) -> Any:
    """
    Create a DashScope (阿里云百炼) LLM instance.

    Uses OpenAI-compatible API endpoint.
    Supported models: qwen-plus, qwen-turbo, qwen-max, qwen-vl-max (multimodal)

    Args:
        model: Model name (e.g., "qwen-plus", "qwen-vl-max")
        temperature: Sampling temperature
        max_tokens: Maximum tokens to generate
        callbacks: Optional list of callback handlers
        metadata: Optional metadata dict for LangSmith tracing
        streaming: Enable streaming mode for the LLM
        **kwargs: Additional parameters to pass to the LLM

    Returns:
        ChatOpenAI instance (DashScope uses OpenAI-compatible API)
    """
    try:
        from langchain_openai import ChatOpenAI
    except ImportError:
        from langchain_community.chat_models import ChatOpenAI

    api_key = get_api_key(LLMProvider.DASHSCOPE)

    # Use DashScope OpenAI-compatible endpoint
    from .config import DASHSCOPE_API_BASE
    kwargs['base_url'] = DASHSCOPE_API_BASE

    # Merge callbacks with LangSmith callbacks
    all_callbacks = _merge_callbacks(callbacks)

    # Only pass callbacks if we have them
    if all_callbacks:
        kwargs['callbacks'] = all_callbacks

    # Pass metadata if provided
    if metadata:
        kwargs['metadata'] = metadata

    logger.info(f"[DashScope] Using API base: {DASHSCOPE_API_BASE}")

    return ChatOpenAI(
        model=model,
        temperature=temperature,
        max_tokens=max_tokens,
        api_key=api_key,
        streaming=streaming,
        **kwargs
    )
```

**Step 2: 验证函数定义**

```bash
python -c "from src.core.llm_factory import _create_dashscope_llm; print('DashScope LLM factory function loaded')"
```

Expected: `DashScope LLM factory function loaded`

---

### Task 6: 更新 create_llm 路由支持 DashScope

**Files:**
- Modify: `src/core/llm_factory.py:296-317`

**Step 1: 更新 provider 路由逻辑**

将第 297-307 行：
```python
    # Route to appropriate provider-specific function
    if selected_provider == LLMProvider.ZHIPUAI:
        return _create_zhipuai_llm(
            model=model,
            temperature=temperature,
            max_tokens=max_tokens,
            callbacks=callbacks,
            metadata=metadata,
            streaming=streaming,
            **kwargs
        )
    else:  # OPENAI (includes DeepSeek)
        return _create_openai_llm(
```

改为：
```python
    # Route to appropriate provider-specific function
    if selected_provider == LLMProvider.DASHSCOPE:
        return _create_dashscope_llm(
            model=model,
            temperature=temperature,
            max_tokens=max_tokens,
            callbacks=callbacks,
            metadata=metadata,
            streaming=streaming,
            **kwargs
        )
    elif selected_provider == LLMProvider.ZHIPUAI:
        return _create_zhipuai_llm(
            model=model,
            temperature=temperature,
            max_tokens=max_tokens,
            callbacks=callbacks,
            metadata=metadata,
            streaming=streaming,
            **kwargs
        )
    else:  # OPENAI (includes DeepSeek)
        return _create_openai_llm(
```

**Step 2: 更新日志输出**

将第 295 行：
```python
    logger.info(f"[LLM Factory] Creating LLM: model={model}, provider={selected_provider.value}, streaming={streaming}")
```

改为：
```python
    logger.info(f"[LLM Factory] Creating LLM: model={model}, provider={selected_provider.value}, streaming={streaming}")

    # Log special note for DashScope
    if selected_provider == LLMProvider.DASHSCOPE:
        logger.info(f"[LLM Factory] Using DashScope (阿里云百炼) OpenAI-compatible API")
```

**Step 3: 验证 DashScope LLM 创建**

```bash
python -c "
from src.core.llm_factory import create_llm
llm = create_llm(model='qwen-plus', provider='dashscope', temperature=0.7)
print(f'LLM created: {type(llm).__name__}')
print(f'Model: {llm.model_name}')
"
```

Expected: `LLM created: ChatOpenAI` 和 `Model: qwen-plus`

---

### Task 7: 更新 .env 配置模板

**Files:**
- Modify: `.env`

**Step 1: 更新 LLM 配置区域**

将第 24-31 行：
```
# ============================================
# LLM Configuration (阿里云百炼 OpenAI 兼容接口)
# ============================================
# 文档：https://help.aliyun.com/zh/model-studio/compatibility-of-openai-with-dashscope
# 支持模型：deepseek-chat, qwen-plus, qwen-turbo, qwen-max 等
OPENAI_API_KEY=sk-2268e2673ae0427cb86a3a6c6fe5d138
OPENAI_API_BASE=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat
MAX_TOKENS=65536
```

改为：
```
# ============================================
# LLM Configuration (阿里云百炼 OpenAI 兼容接口)
# ============================================
# 文档：https://help.aliyun.com/zh/model-studio/compatibility-of-openai-with-dashscope
# 支持模型：deepseek-chat, qwen-plus, qwen-turbo, qwen-max, qwen-vl-max (多模态)
OPENAI_API_KEY=sk-2268e2673ae0427cb86a3a6c6fe5d138
OPENAI_API_BASE=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat
MAX_TOKENS=65536

# ============================================
# DashScope Provider Configuration (qwen3.5)
# ============================================
# 使用 DashScope provider 时设置
LLM_PROVIDER=dashscope
# 支持模型：qwen-plus (推荐), qwen-turbo, qwen-max, qwen-vl-max (多模态)
DASHSCOPE_MODEL=qwen-plus
# DASHSCOPE_API_KEY 已在上面配置
# DASHSCOPE_API_BASE 默认使用 OpenAI 兼容模式
```

**Step 2: 验证 .env 语法**

```bash
python -c "from dotenv import load_dotenv; load_dotenv(); import os; print(f'LLM_PROVIDER={os.getenv(\"LLM_PROVIDER\")}')"
```

Expected: `LLM_PROVIDER=dashscope` (如果已设置) 或 `LLM_PROVIDER=None`

---

## Phase 2: 网络搜索功能

### Task 8: 创建网络搜索工具文件

**Files:**
- Create: `src/tools/search_tool.py`

**Step 1: 创建基础搜索工具框架**

```python
"""
网络搜索工具 - 支持 Agent 自主联网获取信息

支持多种搜索后端:
- Tavily Search API (推荐，简单可靠)
- Bing Search API
- 阿里云搜索 API
"""

from typing import Any, Dict, List, Optional
from ..utils.logger import get_logger

logger = get_logger(__name__)


class SearchBackend:
    """搜索后端接口"""

    def search(self, query: str, num_results: int = 5) -> List[Dict[str, Any]]:
        """
        执行搜索并返回结果

        Args:
            query: 搜索查询
            num_results: 返回结果数量

        Returns:
            搜索结果列表，每项包含 title, url, snippet
        """
        raise NotImplementedError


class TavilySearchBackend(SearchBackend):
    """Tavily Search API 后端"""

    def __init__(self, api_key: Optional[str] = None):
        self.api_key = api_key or os.getenv("TAVILY_API_KEY")
        if not self.api_key:
            raise ValueError("TAVILY_API_KEY not configured")

    def search(self, query: str, num_results: int = 5) -> List[Dict[str, Any]]:
        """使用 Tavily API 执行搜索"""
        try:
            from tavily import TavilyClient

            client = TavilyClient(api_key=self.api_key)
            response = client.search(query, max_results=num_results)

            results = []
            for result in response.get("results", []):
                results.append({
                    "title": result.get("title", "无标题"),
                    "url": result.get("url", ""),
                    "snippet": result.get("content", ""),
                    "score": result.get("score", 0)
                })
            return results

        except ImportError:
            logger.warning("Tavily 客户端未安装，使用 HTTP 请求方式")
            return self._search_via_http(query, num_results)
        except Exception as e:
            logger.error(f"Tavily 搜索失败：{e}")
            return []

    def _search_via_http(self, query: str, num_results: int = 5) -> List[Dict[str, Any]]:
        """使用 HTTP 请求调用 Tavily API"""
        import requests

        url = "https://api.tavily.com/search"
        payload = {
            "api_key": self.api_key,
            "query": query,
            "max_results": num_results,
            "search_depth": "basic"
        }

        try:
            response = requests.post(url, json=payload, timeout=10)
            response.raise_for_status()
            data = response.json()

            results = []
            for result in data.get("results", []):
                results.append({
                    "title": result.get("title", "无标题"),
                    "url": result.get("url", ""),
                    "snippet": result.get("content", ""),
                    "score": result.get("score", 0)
                })
            return results
        except Exception as e:
            logger.error(f"Tavily HTTP 请求失败：{e}")
            return []


class BingSearchBackend(SearchBackend):
    """Bing Search API 后端"""

    def __init__(self, api_key: Optional[str] = None, endpoint: Optional[str] = None):
        self.api_key = api_key or os.getenv("BING_SEARCH_API_KEY")
        self.endpoint = endpoint or os.getenv("BING_SEARCH_ENDPOINT")

    def search(self, query: str, num_results: int = 5) -> List[Dict[str, Any]]:
        """使用 Bing API 执行搜索"""
        import requests

        if not self.api_key or not self.endpoint:
            logger.warning("Bing API 未配置")
            return []

        url = f"{self.endpoint}/v7.0/search"
        headers = {"Ocp-Apim-Subscription-Key": self.api_key}
        params = {"q": query, "count": num_results}

        try:
            response = requests.get(url, headers=headers, params=params, timeout=10)
            response.raise_for_status()
            data = response.json()

            results = []
            for result in data.get("webPages", {}).get("value", []):
                results.append({
                    "title": result.get("name", "无标题"),
                    "url": result.get("url", ""),
                    "snippet": result.get("snippet", ""),
                    "date": result.get("dateLastCrawled", "")
                })
            return results
        except Exception as e:
            logger.error(f"Bing 搜索失败：{e}")
            return []


def get_search_backend(backend: str = "tavily") -> SearchBackend:
    """获取搜索后端实例"""
    if backend == "tavily":
        return TavilySearchBackend()
    elif backend == "bing":
        return BingSearchBackend()
    else:
        raise ValueError(f"不支持的搜索后端：{backend}")


def format_search_results(results: List[Dict[str, Any]], max_results: int = 5) -> str:
    """格式化搜索结果"""
    if not results:
        return "未找到相关搜索结果"

    formatted = ["## 网络搜索结果\n"]
    for i, result in enumerate(results[:max_results], 1):
        formatted.append(f"**{i}. {result['title']}**")
        formatted.append(f"   URL: {result['url']}")
        formatted.append(f"   {result['snippet']}\n")

    return "\n".join(formatted)
```

**Step 2: 验证语法**

```bash
python -m py_compile src/tools/search_tool.py
```

Expected: 无错误

---

### Task 9: 注册 web_search 工具到 ToolRegistry

**Files:**
- Modify: `src/tools/registry.py`
- Create: `src/tools/search_tool.py` (已在上一个 task 创建)

**Step 1: 在 registry.py 中导入并注册搜索工具**

在 `src/tools/registry.py` 第 321 行（knowledge_search_tool 之后）添加：

```python
@ToolRegistry.register("web_search")
def web_search_tool(context: Dict[str, Any]) -> str:
    """
    网络搜索工具

    从互联网搜索实时信息，支持新闻、政策、技术数据等查询。

    Args:
        context: 包含查询参数的上下文字典
            - query: 搜索查询字符串（必需）
            - backend: 搜索后端（可选，默认"tavily"）
            - num_results: 返回结果数量（可选，默认 5）

    Returns:
        格式化的网络搜索结果
    """
    try:
        from .search_tool import get_search_backend, format_search_results

        query = context.get("query", "")
        backend = context.get("backend", "tavily")
        num_results = context.get("num_results", 5)

        if not query:
            return "## 网络搜索错误\n\n错误：缺少查询参数 'query'"

        logger.info(f"[web_search] 执行搜索：query='{query[:50]}...', backend={backend}")

        search_backend = get_search_backend(backend)
        results = search_backend.search(query, num_results=num_results)

        return format_search_results(results, max_results=num_results)

    except ImportError as e:
        logger.error(f"[web_search] 搜索模块导入失败：{e}")
        return f"## 网络搜索错误\n\n错误：搜索模块未正确安装 - {str(e)}"
    except Exception as e:
        logger.error(f"[web_search] 搜索失败：{e}")
        return f"## 网络搜索错误\n\n错误：{str(e)}"
```

**Step 2: 验证工具注册**

```bash
python -c "from src.tools.registry import ToolRegistry; print('web_search' in ToolRegistry.list_tools())"
```

Expected: `True`

---

### Task 10: 添加搜索工具依赖配置

**Files:**
- Modify: `requirements.txt` (如果存在)
- Modify: `.env` (添加 API Key 配置)

**Step 1: 在 .env 添加搜索 API 配置**

在 `.env` 文件末尾添加：

```
# ============================================
# Web Search Configuration
# ============================================
# Tavily Search API (推荐，每月 1000 次免费搜索)
# 获取 API Key: https://app.tavily.com
TAVILY_API_KEY=your_tavily_api_key_here

# Bing Search API (备选方案)
# BING_SEARCH_API_KEY=your_bing_api_key
# BING_SEARCH_ENDPOINT=https://api.bing.microsoft.com
```

**Step 2: 验证 .env 配置**

```bash
python -c "from dotenv import load_dotenv; load_dotenv(); import os; print(f'TAVILY_API_KEY configured: {os.getenv(\"TAVILY_API_KEY\", \"not set\")[:10]}...')"
```

---

## Phase 3: 测试与验证

### Task 11: 测试 qwen3.5 LLM 调用

**Files:**
- Create: `tests/test_dashscope_llm.py`

**Step 1: 创建 DashScope LLM 测试**

```python
"""
DashScope (qwen3.5) LLM 测试
"""

import pytest
from src.core.llm_factory import create_llm, LLMProvider, get_provider
from langchain_core.messages import HumanMessage


class TestDashScopeProvider:
    """DashScope provider 测试"""

    def test_provider_routing(self):
        """测试 provider 路由"""
        assert get_provider("dashscope") == LLMProvider.DASHSCOPE

    def test_llm_creation(self):
        """测试 LLM 实例创建"""
        llm = create_llm(
            model="qwen-plus",
            provider="dashscope",
            temperature=0.7,
            max_tokens=100
        )
        assert llm is not None
        assert llm.model_name == "qwen-plus"

    def test_llm_inference(self):
        """测试实际推理调用"""
        llm = create_llm(
            model="qwen-plus",
            provider="dashscope",
            temperature=0.7,
            max_tokens=50
        )

        response = llm.invoke([HumanMessage(content="你好，请用一句话介绍你自己")])
        assert response is not None
        assert len(response.content) > 0
        print(f"\nQwen 回复：{response.content}")
```

**Step 2: 运行测试**

```bash
pytest tests/test_dashscope_llm.py -v
```

Expected: 所有测试通过

---

### Task 12: 测试网络搜索工具

**Files:**
- Create: `tests/test_search_tool.py`

**Step 1: 创建搜索工具测试**

```python
"""
网络搜索工具测试
"""

import pytest
from src.tools.search_tool import (
    TavilySearchBackend,
    BingSearchBackend,
    get_search_backend,
    format_search_results
)


class TestSearchBackend:
    """搜索后端测试"""

    def test_tavily_backend_creation(self):
        """测试 Tavily 后端创建"""
        try:
            backend = TavilySearchBackend()
            assert backend is not None
        except ValueError:
            pytest.skip("TAVILY_API_KEY not configured")

    def test_tavily_search(self):
        """测试 Tavily 搜索功能"""
        try:
            backend = TavilySearchBackend()
            results = backend.search("乡村振兴政策 2025", num_results=3)
            assert isinstance(results, list)
            assert len(results) > 0
            assert "title" in results[0]
            assert "url" in results[0]
            print(f"\n搜索结果：{results[0]['title']}")
        except ValueError:
            pytest.skip("TAVILY_API_KEY not configured")

    def test_format_results(self):
        """测试结果格式化"""
        mock_results = [
            {
                "title": "测试标题",
                "url": "https://example.com",
                "snippet": "测试摘要内容",
                "score": 0.95
            }
        ]
        formatted = format_search_results(mock_results)
        assert "## 网络搜索结果" in formatted
        assert "测试标题" in formatted
        assert "https://example.com" in formatted

    def test_web_search_tool_via_registry(self):
        """测试通过 ToolRegistry 调用搜索工具"""
        from src.tools.registry import ToolRegistry

        try:
            result = ToolRegistry.execute_tool(
                "web_search",
                {"query": "村庄规划 最新政策", "num_results": 2}
            )
            assert isinstance(result, str)
            print(f"\n工具返回：{result[:200]}...")
        except ValueError as e:
            pytest.skip(f"搜索工具不可用：{e}")
```

**Step 2: 运行测试**

```bash
pytest tests/test_search_tool.py -v
```

Expected: 跳过或所有测试通过

---

### Task 13: 集成测试 - Agent 使用新工具

**Files:**
- Create: `tests/test_integration.py`

**Step 1: 创建集成测试**

```python
"""
集成测试 - 测试 Agent 使用 qwen3.5 + 网络搜索
"""

import pytest
from src.core.llm_factory import create_llm
from src.tools.registry import ToolRegistry
from langchain_core.messages import HumanMessage, SystemMessage


class TestIntegration:
    """集成测试"""

    def test_qwen_with_search_context(self):
        """测试 qwen3.5 结合搜索上下文"""
        # 先执行搜索
        try:
            search_result = ToolRegistry.execute_tool(
                "web_search",
                {"query": "2025 年 村庄规划 政策", "num_results": 3}
            )
        except ValueError:
            pytest.skip("搜索工具不可用")

        # 创建 qwen LLM
        llm = create_llm(
            model="qwen-plus",
            provider="dashscope",
            temperature=0.7,
            max_tokens=500
        )

        # 构建提示
        messages = [
            SystemMessage(content="你是一个村庄规划助手，请基于提供的信息回答问题。"),
            HumanMessage(content=f"""
请根据以下网络搜索结果，总结 2025 年村庄规划的相关政策：

{search_result}

请用简洁的语言总结关键政策要点。
""")
        ]

        response = llm.invoke(messages)
        assert response is not None
        assert len(response.content) > 0
        print(f"\nAI 总结:\n{response.content}")
```

**Step 2: 运行集成测试**

```bash
pytest tests/test_integration.py -v -s
```

Expected: 测试通过并打印 AI 总结

---

## 验收标准检查

### Task 14: 验证所有验收标准

**Checklist:**

1. ✅ 可通过 `.env` 配置使用 qwen3.5 模型
   - 设置 `LLM_PROVIDER=dashscope`
   - 设置 `DASHSCOPE_MODEL=qwen-plus`

2. ✅ 网络搜索工具可正常调用并返回结果
   - `ToolRegistry.execute_tool("web_search", {...})` 正常工作

3. ✅ 不引入大量新依赖
   - Phase 1: 零新增
   - Phase 2: 可选 `tavily-python` (1 个新包)

4. ✅ 现有功能不受影响（向后兼容）
   - 运行现有测试确保无破坏性变更

5. ✅ 提供清晰的配置和使用文档
   - 更新 README.md

**Step 1: 运行完整测试套件**

```bash
pytest tests/test_dashscope_llm.py tests/test_search_tool.py tests/test_integration.py -v
```

**Step 2: 验证向后兼容性**

```bash
pytest tests/ -k "not test_" --collect-only  # 查看现有测试
pytest tests/ -x  # 运行现有测试，确保无破坏
```

---

## 使用文档

### 配置 qwen3.5

```bash
# .env
LLM_PROVIDER=dashscope
DASHSCOPE_MODEL=qwen-plus  # 或 qwen-turbo, qwen-max, qwen-vl-max
DASHSCOPE_API_KEY=sk-your-api-key
```

### 使用网络搜索

```python
from src.tools.registry import ToolRegistry

# 执行网络搜索
result = ToolRegistry.execute_tool(
    "web_search",
    {
        "query": "村庄规划 最新政策 2025",
        "backend": "tavily",  # 或 "bing"
        "num_results": 5
    }
)
print(result)
```

### 在 Agent 中使用

在 Agent 的 tool 列表中添加 `"web_search"`，Agent 即可自主调用网络搜索。

---

## 依赖清单

| 阶段 | 新增依赖 | 用途 |
|------|----------|------|
| Phase 1 | 无 | 复用现有 `langchain-openai` |
| Phase 2 | `tavily-python`（可选） | 网络搜索 API |
| Phase 3 | `geopandas`（可选） | GIS 空间分析 |

---

## 风险与缓解

| 风险 | 缓解措施 |
|------|----------|
| DashScope API 不稳定 | 降级到现有 provider |
| Tavily API 配额限制 | 支持 Bing 备选方案 |
| 网络搜索延迟 | 设置 timeout，异步执行 |

---

## 后续扩展

1. **多模态支持**: 使用 qwen-vl-max 处理图像输入
2. **RAG 增强**: 网络搜索 + 本地知识库混合检索
3. **GIS 在线数据**: 加载 OpenStreetMap 等在线 GIS 数据源
