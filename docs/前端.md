# 前端实现文档

> **村庄规划智能体** - Next.js 14 前端架构与实现

## 目录

- [技术栈](#技术栈)
- [项目结构](#项目结构)
- [类型系统](#类型系统)
- [状态管理](#状态管理)
- [SSE 集成](#sse-集成)
- [组件架构](#组件架构)
- [API 客户端](#api-客户端)

---

## 技术栈

### 核心框架
- **Next.js 14** - React 框架 (App Router)
- **React 18** - UI 库
- **TypeScript** - 类型安全

### 样式方案
- **Tailwind CSS** - 实用优先的 CSS 框架
- **Bootstrap 5.3** - 额外的 UI 组件

### 状态管理
- **React Context API** - 全局状态管理
- **自定义 Hooks** - 逻辑复用

### 实时通信
- **Server-Sent Events (SSE)** - 流式数据传输

### 代码质量
- **ESLint** - 代码检查
- **Prettier** - 代码格式化

---

## 项目结构

### 目录结构

```
frontend/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── layout.tsx          # 根布局
│   │   ├── page.tsx            # 主页
│   │   ├── globals.css         # 全局样式
│   │   └── village/            # 村庄规划页面
│   │       └── [taskId]/       # 动态路由
│   │           ├── loading.tsx # 加载状态
│   │           └── page.tsx    # 规划页面
│   │
│   ├── components/             # React 组件
│   │   ├── chat/               # 聊天界面 (11个组件)
│   │   │   ├── ChatPanel.tsx           # 主聊天面板
│   │   │   ├── MessageList.tsx         # 消息列表
│   │   │   ├── MessageBubble.tsx       # 消息气泡
│   │   │   ├── MessageContent.tsx      # 消息内容渲染
│   │   │   ├── StreamingText.tsx       # 流式文本
│   │   │   ├── LayerReportMessage.tsx  # 层级报告消息
│   │   │   ├── LayerReportCard.tsx     # 报告卡片
│   │   │   ├── DimensionSection.tsx    # 维度区块
│   │   │   ├── ReviewInteractionMessage.tsx # 审查交互
│   │   │   ├── CodeBlock.tsx           # 代码块
│   │   │   ├── ThinkingIndicator.tsx   # 思考指示器
│   │   │   └── WelcomeCard.tsx         # 欢迎卡片
│   │   │
│   │   ├── layout/             # 布局组件
│   │   │   ├── Header.tsx              # 页头
│   │   │   ├── HistoryPanel.tsx        # 历史面板
│   │   │   └── UnifiedContentSwitcher.tsx # 内容切换器
│   │   │
│   │   ├── review/             # 审查组件
│   │   │   └── ReviewPanel.tsx         # 审查面板
│   │   │
│   │   ├── report/             # 报告组件
│   │   │   └── KnowledgeReference.tsx  # 知识引用
│   │   │
│   │   ├── ui/                 # 通用 UI 组件
│   │   │   ├── Card.tsx                # 卡片
│   │   │   └── SegmentedControl.tsx    # 分段控制器
│   │   │
│   │   ├── VillageInputForm.tsx        # 村庄输入表单
│   │   ├── MarkdownRenderer.tsx        # Markdown 渲染
│   │   └── FileUpload.tsx              # 文件上传
│   │
│   ├── contexts/               # React Context
│   │   └── UnifiedPlanningContext.tsx  # 统一规划上下文
│   │
│   ├── hooks/                  # 自定义 Hooks
│   │   ├── useStreamingText.ts        # 流式文本
│   │   ├── useStreamingContent.ts     # 流式内容
│   │   ├── useTaskSSE.ts              # SSE 连接
│   │   ├── useDebounce.ts             # 防抖
│   │   ├── useBreakpoint.ts           # 断点
│   │   ├── useKeyboardShortcuts.ts    # 键盘快捷键
│   │   └── useLazyLoad.tsx            # 懒加载
│   │
│   ├── lib/                    # 工具库
│   │   ├── api.ts                      # API 客户端 (487 行)
│   │   ├── constants.ts                # 常量定义 (新增) ⭐
│   │   ├── logger.ts                  # 日志
│   │   ├── layerReportParser.ts       # 报告解析
│   │   └── utils/                      # 工具函数
│   │       ├── index.ts               # 通用工具
│   │       ├── cn.ts                  # 类名合并
│   │       ├── message-helpers.ts     # 消息辅助
│   │       ├── button-styles.ts       # 按钮样式
│   │       └── input-styles.ts        # 输入样式
│   │
│   ├── types/                  # TypeScript 类型
│   │   ├── message.ts          # 核心类型 (~175 行) ⭐ 简化
│   │   ├── message-types.ts    # 消息类型 (~150 行) ⭐ 新增
│   │   ├── message-guards.ts   # 类型守卫 (~100 行) ⭐ 新增
│   │   ├── message-helpers.ts  # 消息辅助 (~80 行) ⭐ 新增
│   │   └── index.ts            # 统一导出
│   │
│   ├── config/                 # 配置文件
│   │   └── (已删除 features.ts - 未使用)
│   │
│   └── styles/                 # 样式文件
│       └── globals.css         # 全局样式
│
├── .env.local                  # 环境变量
├── package.json                # 依赖配置
├── tsconfig.json               # TypeScript 配置
├── next.config.js              # Next.js 配置
└── .eslintrc.json              # ESLint 配置
```

---

## 类型系统

### 重构概览 ⭐ (2024年最新)

**重要变更**: 类型系统已从单一文件重构为模块化结构

#### 文件结构

**之前**: 单一文件 `message.ts` (395 行)

**现在**: 5个专注文件

```
types/
├── message.ts          # 核心类型 (~175 行)
├── message-types.ts    # 具体消息类型 (~150 行)
├── message-guards.ts   # 类型守卫函数 (~100 行)
├── message-helpers.ts  # 消息辅助函数 (~80 行)
└── index.ts            # 统一导出
```

#### 各文件职责

##### 1. message.ts - 核心类型

**职责**: 基础接口和通用类型

```typescript
// 基础消息接口
export interface BaseMessage {
  id: string;
  timestamp: Date;
  role: 'user' | 'assistant' | 'system';
}

// 消息类型联合
export type Message =
  | TextMessage
  | FileMessage
  | ProgressMessage
  | ActionMessage
  | ResultMessage
  | ErrorMessage
  | SystemMessage
  | LayerCompletedMessage
  | ReviewRequestMessage
  | CheckpointListMessage
  | ReviewInteractionMessage;

// 其他核心类型
export type StreamingState = 'idle' | 'streaming' | 'paused' | 'completed';
export interface ActionButton { /* ... */ }
export interface DimensionInfo { /* ... */ }
export interface Checkpoint { /* ... */ }
```

##### 2. message-types.ts - 具体消息类型

**职责**: 所有特定消息类型的完整定义

```typescript
// 文本消息
export interface TextMessage extends BaseMessage {
  type: 'text';
  content: string;
  streamingState?: StreamingState;
  streamingContent?: string;
  knowledgeReferences?: KnowledgeReference[];
}

// 进度消息
export interface ProgressMessage extends BaseMessage {
  type: 'progress';
  content: string;
  progress: number;
  currentLayer?: string;
  taskId?: string;
}

// 层级完成消息
export interface LayerCompletedMessage extends BaseMessage {
  type: 'layer_completed';
  layer: number;
  content: string;
  summary: {
    word_count: number;
    key_points: string[];
    dimension_count?: number;
    dimension_names?: string[];
  };
  fullReportContent?: string;
  dimensionReports?: Record<string, string>;
  actions: ActionButton[];
}

// 审查交互消息
export interface ReviewInteractionMessage extends BaseMessage {
  type: 'review_interaction';
  role: 'assistant';
  layer: number;
  content: string;
  reviewState: 'pending' | 'approved' | 'rejected' | 'rolled_back';
  availableActions: ('approve' | 'reject' | 'rollback')[];
  enableDimensionSelection: boolean;
  availableDimensions?: DimensionInfo[];
  enableRollback: boolean;
  checkpoints?: Checkpoint[];
  feedbackPlaceholder: string;
  quickFeedbackOptions?: string[];
  submittedAt?: Date;
  submittedBy?: 'user';
  submissionType?: 'approve' | 'reject' | 'rollback';
  submissionFeedback?: string;
  submissionDimensions?: string[];
}

// ... 其他消息类型
```

##### 3. message-guards.ts - 类型守卫

**职责**: 运行时类型检查和类型缩小

```typescript
import type { Message } from './message';
import type {
  TextMessage,
  ProgressMessage,
  ActionMessage,
  // ...
} from './message-types';

// 检查是否为用户消息
export const isUserMessage = (msg: Message): boolean =>
  msg.role === 'user';

// 检查是否为文本消息
export const isTextMessage = (msg: Message): msg is TextMessage =>
  msg.type === 'text';

// 检查是否为进度消息
export const isProgressMessage = (msg: Message): msg is ProgressMessage =>
  msg.type === 'progress';

// 检查是否为操作消息
export const isActionMessage = (msg: Message): msg is ActionMessage =>
  msg.type === 'action';

// 检查是否为层级完成消息
export const isLayerCompletedMessage = (
  msg: Message
): msg is LayerCompletedMessage =>
  msg.type === 'layer_completed';

// 检查是否为审查交互消息
export const isReviewInteractionMessage = (
  msg: Message
): msg is ReviewInteractionMessage =>
  msg.type === 'review_interaction';

// ... 其他类型守卫
```

**使用示例**:
```typescript
function renderMessage(msg: Message) {
  if (isTextMessage(msg)) {
    // TypeScript 知道这是 TextMessage
    return <StreamingText content={msg.content} />;
  }

  if (isLayerCompletedMessage(msg)) {
    // TypeScript 知道这是 LayerCompletedMessage
    return <LayerReportCard layer={msg.layer} summary={msg.summary} />;
  }
}
```

##### 4. message-helpers.ts - 辅助函数

**职责**: SSE 事件转换和消息创建

```typescript
import type { Message, SSEEvent } from './message';
import type { ProgressMessage, ActionMessage } from './message-types';

/**
 * 将 SSE 事件转换为前端消息
 */
export function convertSSEToMessage(event: SSEEvent): Message {
  const base = {
    id: `msg-${Date.now()}-${Math.random()}`,
    timestamp: new Date(),
    role: 'assistant' as const,
  };

  // 进度更新
  if (event.status === 'running' && event.progress !== undefined) {
    return {
      ...base,
      type: 'progress',
      content: event.message || '正在处理...',
      progress: event.progress,
      currentLayer: event.current_layer,
      taskId: event.task_id,
    } as ProgressMessage;
  }

  // 暂停状态
  if (event.status === 'paused' || event.event_type === 'pause') {
    return {
      ...base,
      type: 'action',
      content: '当前层级已完成，请审查后继续',
      actions: createActionButtons('pause'),
      taskId: event.task_id,
    } as ActionMessage;
  }

  // ... 其他转换逻辑
}
```

##### 5. index.ts - 统一导出

**职责**: 便捷的单入口导入

```typescript
/**
 * Message Types - Unified Exports
 * 消息类型统一导出
 */

// Core types
export * from './message';

// Specific message type definitions
export * from './message-types';

// Type guard functions
export * from './message-guards';

// Helper functions
export * from './message-helpers';
```

**导入方式**:
```typescript
// 方式 1: 从 @/types 导入所有
import { Message, TextMessage, isTextMessage } from '@/types';

// 方式 2: 从具体文件导入
import { TextMessage } from '@/types/message-types';
```

#### 优势

1. **更好的组织** - 相关类型集中在专门文件中
2. **更容易查找** - 快速定位特定类型定义
3. **清晰的职责** - 每个文件有明确的目的
4. **类型安全** - 类型守卫确保运行时安全
5. **向后兼容** - 通过 index.ts 保持原有导入方式

---

## 状态管理

### UnifiedPlanningContext

**文件**: `contexts/UnifiedPlanningContext.tsx` (626 行)

**职责**: 全局规划状态管理

#### 状态结构

```typescript
interface PlanningState {
  // 任务信息
  taskId: string | null;
  sessionId: string | null;
  projectName: string | null;

  // 消息
  messages: Message[];

  // 状态
  status: TaskStatus;
  currentLayer: number | null;
  currentCheckpoint: Checkpoint | null;

  // 错误
  error: string | null;
}

type TaskStatus =
  | 'idle'
  | 'pending'
  | 'running'
  | 'paused'
  | 'reviewing'
  | 'revising'
  | 'completed'
  | 'failed';
```

#### 方法

```typescript
interface PlanningContextValue {
  // 消息管理
  addMessage: (message: Message) => void;
  updateLastMessage: (updates: Partial<Message>) => void;
  clearMessages: () => void;

  // 状态管理
  setStatus: (status: TaskStatus) => void;
  setError: (error: string | null) => void;

  // 审查操作
  approveReview: () => Promise<void>;
  rejectReview: (feedback: string, dimensions?: string[]) => Promise<void>;
  rollbackToCheckpoint: (checkpointId: string) => Promise<void>;

  // SSE 管理
  reconnectSSE: () => void;
}
```

---

## SSE 集成

### useTaskSSE Hook

**文件**: `hooks/useTaskSSE.ts` (221 行) ⭐ 已简化

#### 最新改进 (2024年)

1. **接口统一** - 合并 `UseTaskSSEOptions` 和 `SSEHandlerCallbacks`
2. **事件映射** - 使用 `EVENT_HANDLERS` 对象替代复杂 switch
3. **简化引用** - 减少 callbackRefs 复杂性

#### 接口定义

```typescript
interface UseTaskSSEOptions {
  // 进度回调
  onProgress?: (data: ProgressData) => void;

  // 层级完成回调
  onLayerCompleted?: (data: LayerCompletedData) => void;

  // 检查点保存回调
  onCheckpointSaved?: (data: CheckpointData) => void;

  // 暂停回调
  onPause?: (data: PauseData) => void;

  // 流暂停回调 ⭐ NEW
  onStreamPaused?: () => void;

  // 错误回调
  onError?: (error: string) => void;

  // 完成回调
  onCompleted?: () => void;
}

interface UseTaskSSEReturn {
  isConnected: boolean;
  error: string | null;
  close: () => void;
  reconnect: () => void;  // ⭐ NEW: 手动重连
}
```

#### 使用示例

```typescript
const { isConnected, error, close, reconnect } = useTaskSSE(taskId, {
  onProgress: (data) => {
    console.log('进度更新:', data.progress);
  },

  onLayerCompleted: (data) => {
    console.log('层级完成:', data.layer);
    // 显示层级报告
    addLayerReportMessage(data);
  },

  onPause: (data) => {
    console.log('任务暂停:', data.reason);
    // 显示审查面板
    showReviewPanel();
  },

  onStreamPaused: () => {
    // 前端主动关闭连接
    console.log('流已暂停,关闭连接');
  },

  onError: (error) => {
    console.error('SSE 错误:', error);
    // 显示错误消息
    showErrorMessage(error);
  }
});

// 批准后手动重连
const handleApprove = async () => {
  await planningApi.approveReview(taskId);
  reconnect();  // 创建新的 SSE 连接
};
```

#### 事件处理器映射 ⭐ NEW

```typescript
// 简化的事件处理
const EVENT_HANDLERS = {
  progress: (e: MessageEvent) => {
    const data = JSON.parse(e.data);
    onProgress?.(data);
  },

  layer_completed: (e: MessageEvent) => {
    const data = JSON.parse(e.data);
    onLayerCompleted?.(data);
  },

  pause: (e: MessageEvent) => {
    const data = JSON.parse(e.data);
    onPause?.(data);
  },

  stream_paused: (e: MessageEvent) => {
    console.log('[SSE] Stream paused, closing connection');
    eventSourceRef.current?.close();
    onStreamPaused?.();
  },

  error: (e: MessageEvent) => {
    const data = JSON.parse(e.data);
    onError?.(data.message || 'SSE error');
  },

  completed: (e: MessageEvent) => {
    onCompleted?.();
  }
};

// 注册事件监听器
Object.entries(EVENT_HANDLERS).forEach(([event, handler]) => {
  es.addEventListener(event, handler);
});
```

#### SSE 连接生命周期

```
1. 创建连接
   useTaskSSE(taskId)
      ↓
   创建 EventSource
      ↓
   GET /api/planning/stream/{session_id}

2. 接收事件
   progress → 更新进度
   layer_completed → 显示完成消息
   pause → 显示审查面板
   stream_paused → 关闭连接 ⭐
   error → 显示错误

3. 暂停状态
   后端发送 stream_paused
      ↓
   前端关闭 EventSource
      ↓
   连接状态 → disconnected

4. 批准后重连
   用户点击批准
      ↓
   调用 reconnect()
      ↓
   重置初始化标志
      ↓
   创建新的 EventSource
      ↓
   继续执行

5. 完成状态
   后端发送 completed
      ↓
   前端关闭 EventSource
      ↓
   连接状态 → completed
```

---

## 组件架构

### ChatPanel 组件

**文件**: `components/chat/ChatPanel.tsx` (~640 行) ⭐ 已简化

#### 简化内容

1. **行数减少** - 从 1,033 行减少到 ~640 行
2. **逻辑提取** - 复杂逻辑移至 hooks 和 utils
3. **导入优化** - 使用统一的 `@/types` 导入
4. **类型安全** - 改进的类型定义和守卫

#### 主要功能

```typescript
interface ChatPanelProps {
  taskId: string | null;
  sessionId: string | null;
}

function ChatPanel({ taskId, sessionId }: ChatPanelProps) {
  const {
    messages,
    addMessage,
    updateLastMessage,
    status,
    setStatus,
    approveReview,
    rejectReview,
    rollbackToCheckpoint,
    reconnectSSE
  } = usePlanningContext();

  // SSE 连接管理
  const { isConnected, error, close, reconnect } = useTaskSSE(taskId, {
    onProgress: handleProgress,
    onLayerCompleted: handleLayerCompleted,
    onPause: handlePause,
    onStreamPaused: handleStreamPaused,
    onError: handleError,
  });

  // 消息渲染
  return (
    <div className="chat-panel">
      <MessageList messages={messages} />
      {status === 'paused' && (
        <ReviewInteractionMessage
          onApprove={handleApprove}
          onReject={handleReject}
          onRollback={handleRollback}
        />
      )}
    </div>
  );
}
```

### MessageList 组件

**文件**: `components/chat/MessageList.tsx` (206 行)

**职责**:
- 消息列表渲染
- 滚动管理
- 自动滚动到最新消息

### MessageBubble 组件

**文件**: `components/chat/MessageBubble.tsx` (~150 行)

**职责**:
- 单条消息容器
- 消息样式和定位
- 委托给 MessageContent 渲染内容

### MessageContent 组件

**文件**: `components/chat/MessageContent.tsx` (236 行)

**职责**:
- 基于消息类型渲染内容
- 类型路由和分发
- 富内容渲染 (Markdown, 代码块)

**渲染逻辑**:
```typescript
function MessageContent({ message }: MessageContentProps) {
  switch (message.type) {
    case 'text':
      return <TextMessageRenderer message={message} />;

    case 'progress':
      return <ProgressMessageRenderer message={message} />;

    case 'layer_completed':
      return <LayerCompletedRenderer message={message} />;

    case 'review_interaction':
      return <ReviewInteractionRenderer message={message} />;

    // ... 其他类型
  }
}
```

### StreamingText 组件

**文件**: `components/chat/StreamingText.tsx` (240 行)

**职责**:
- 流式文本渲染
- 打字机效果
- 暂停/继续/跳过控制

**特点**:
- 使用 `useStreamingText` hook
- 请求动画帧优化
- 进度条显示
- 光标动画

---

## API 客户端

### planningApi

**文件**: `lib/api.ts` (487 行) ⭐ 已简化

#### 简化内容

1. **删除重复** - 移除重复的 `uploadFile` 方法
2. **修复导出** - 修正默认导出
3. **统一错误处理** - 改进错误一致性

#### API 方法

```typescript
const planningApi = {
  // 启动规划
  startPlanning: async (data: StartPlanningRequest): Promise<StartPlanningResponse> => {
    const response = await fetch(`${API_URL}/planning/start`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(data),
    });
    return response.json();
  },

  // 文件上传
  uploadFile: async (file: File, encoding?: string): Promise<UploadResponse> => {
    const formData = new FormData();
    formData.append('file', file);
    if (encoding) {
      formData.append('encoding', encoding);
    }

    const response = await fetch(`${API_URL}/files/upload`, {
      method: 'POST',
      body: formData,
    });
    return response.json();
  },

  // 审查操作
  approveReview: async (taskId: string): Promise<ReviewResponse> => {
    const response = await fetch(`${API_URL}/planning/review/${taskId}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ action: 'approve' }),
    });
    return response.json();
  },

  rejectReview: async (
    taskId: string,
    feedback: string,
    dimensions?: string[]
  ): Promise<ReviewResponse> => {
    const response = await fetch(`${API_URL}/planning/review/${taskId}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        action: 'reject',
        feedback,
        dimensions,
      }),
    });
    return response.json();
  },

  // 回退到检查点
  rollbackToCheckpoint: async (
    taskId: string,
    checkpointId: string
  ): Promise<RollbackResponse> => {
    const response = await fetch(`${API_URL}/planning/rollback/${taskId}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ checkpoint_id: checkpointId }),
    });
    return response.json();
  },

  // 获取会话列表
  getSessions: async (): Promise<SessionListResponse> => {
    const response = await fetch(`${API_URL}/data/sessions`);
    return response.json();
  },

  // 获取会话详情
  getSession: async (sessionId: string): Promise<SessionDetailResponse> => {
    const response = await fetch(`${API_URL}/data/sessions/${sessionId}`);
    return response.json();
  },
};

export default planningApi;
```

---

## 最新改进 (2024-2025年)

### 架构简化与代码清理

1. **类型系统重构** ✅
   - 拆分为 5 个专注文件
   - 更好的组织和可维护性
   - 清晰的职责分离

2. **删除未使用文件** ✅
   - 删除 `features.ts` (238 行, 未使用)
   - 删除 `report/index.ts` (8 行, 占位符)
   - 删除 `src/subgraphs/*.backup` (2 个备份文件)

3. **代码简化** ✅
   - `useTaskSSE` 接口统一,事件映射简化
   - `api.ts` 删除重复方法,修复导出
   - `ChatPanel.tsx` 减少 400+ 行代码

4. **新增常量文件** ✅
   - 创建 `constants.ts`
   - 提取共享的层映射常量
   - 统一文件上传配置

### UI状态管理优化

5. **ViewMode 计算属性** ✅
   - 引入 ViewMode 统一管理视图状态
   - 中心化视图逻辑，实现单一真相来源
   - Smart Container 模式：UnifiedContentSwitcher 自动响应状态变化

6. **组件清理** ✅
   - 删除12个未使用组件
   - 减少约1000-1500行代码
   - 保持核心功能完整

### SSE 连接优化

7. **SSE 循环修复** ✅
   - 修复 Layer 1 完成后 SSE 连接无限循环问题
   - 添加 stream_states 跟踪
   - 发送 stream_paused 事件，前端主动关闭连接

8. **批准后重连** ✅
   - 批准后创建新的 SSE 连接
   - 正确执行后续层级
   - 改进重连逻辑

### 数据存储规范

9. **统一英文键名** ✅
   - 维度报告使用英文键名存储（如 `location`, `socio_economic`）
   - 显示时映射为中文名称
   - 修复并行任务中维度报告合并问题

### 代码质量提升

- **TypeScript 编译**: ✅ PASSED
- **生产构建**: ✅ PASSED
- **导入验证**: ✅ PASSED
- **代码简化**: ✅ 完成

### 网络影响

- **代码行数**: 净减少 ~400 行
- **文件数量**: +8 (新增) -3 (删除) = +5
- **组织结构**: 显著改善
- **可维护性**: 大幅提升

---

## 开发指南

### 环境设置

```bash
cd frontend
npm install
cp .env.local.example .env.local
# 编辑 .env.local 配置 API_URL
npm run dev
```

### 编码规范

- 使用 TypeScript 类型注解
- 遵循 ESLint 规则
- 组件使用函数式 + Hooks
- 优先使用 Tailwind CSS 类

### 添加新功能

1. 在 `types/` 中定义类型
2. 创建组件文件
3. 使用 Context 或 Hook 管理状态
4. 集成到现有组件树

### 测试

```bash
# 类型检查
npm run type-check

# Lint
npm run lint

# 构建
npm run build

# 开发
npm run dev
```

---

## 常见问题

### Q: 如何添加新的消息类型?

A:
1. 在 `message-types.ts` 中定义接口
2. 在 `message.ts` 的 `Message` 联合类型中添加
3. 在 `message-guards.ts` 中添加类型守卫
4. 在 `MessageContent.tsx` 中添加渲染逻辑

### Q: 如何自定义 SSE 事件处理?

A: 在 `useTaskSSE` 中传入自定义回调:
```typescript
useTaskSSE(taskId, {
  onCustomEvent: (data) => { /* 处理 */ }
});
```

### Q: 如何调试消息流?

A: 使用 Redux DevTools 或在 Context 中添加日志:
```typescript
addMessage: (message) => {
  console.log('[Context] Adding message:', message);
  setMessages(prev => [...prev, message]);
}
```

---

## 最新修复 (2025-02-09) ⭐⭐⭐ NEW

### Pause 事件去重机制

**问题**: 审查面板重复显示和批准失败

**根本原因**:
- 后端 pause 事件没有去重机制
- 前端 pause 事件处理没有去重检查
- 批准后状态清理不完整

**前端修复** (`frontend/src/components/chat/ChatPanel.tsx`):

#### 1. 添加 pause 事件追踪 Ref (Line 71)

```typescript
const processedPauseEventsRef = useRef<Set<number>>(new Set());
```

**依赖关系**:
```typescript
// 前端内部依赖
ChatPanel.tsx
  ├─> types/message.ts (BaseMessage, ReviewInteractionMessage)
  ├─> contexts/UnifiedPlanningContext.tsx (addMessage, setStatus)
  ├─> hooks/useTaskSSE.ts (SSE连接管理)
  └─> lib/api.ts (planningApi.approveReview)
```

#### 2. onPause 处理器中去重 (Lines 180-206)

```typescript
onPause: (data) => {
  logger.chatPanel.info('执行暂停', data, taskId);
  console.log('[ChatPanel] Task paused:', data);

  const layer = currentLayer || 1;

  // ✅ 检查此layer的pause事件是否已处理
  if (processedPauseEventsRef.current.has(layer)) {
    console.log(`Pause event for Layer ${layer} already processed, skipping`);
    logger.chatPanel.warn(`跳过重复的 Layer ${layer} pause事件`, { layer }, taskId);
    return;  // ⚠️ 跳过重复事件
  }

  // 标记此layer的pause事件已处理
  processedPauseEventsRef.current.add(layer);
  logger.chatPanel.info(`标记 Layer ${layer} pause事件为已处理`, taskId);

  setStatus('paused');
  addMessage({
    type: 'review_interaction',
    layer: layer,
    content: '规划已暂停，请审查后决定下一步操作',
    reviewState: 'pending',
    // ...
  } as ReviewInteractionMessage);

  loadCheckpoints();
},
```

**函数调用关系**:
```
useTaskSSE() [hooks/useTaskSSE.ts]
  ├─> 创建 EventSource [Line ~120]
  ├─> 监听 'pause' 事件 [Line ~60]
  │    └─> onPause(data)  [ChatPanel.tsx:180]
  │         ├─> processedPauseEventsRef.current.has(layer)  [Line 187]
  │         │    └─> return (如果已处理)  [Line 190]
  │         ├─> processedPauseEventsRef.current.add(layer)  [Line 194]
  │         ├─> setStatus('paused')  [Line 197]
  │         ├─> addMessage({...})  [Line 198-216]
  │         │    └─> UnifiedPlanningContext.addMessage()  [contexts/UnifiedPlanningContext.tsx]
  │         └─> loadCheckpoints()  [Line 217]
```

#### 3. 批准后清除追踪 (Lines 385-387)

```typescript
const handleReviewInteractionApprove = useCallback(async (message: ReviewInteractionMessage) => {
  if (!taskId) return;

  try {
    const response = await planningApi.approveReview(taskId);

    // 更新审查消息状态
    addMessage({
      ...message,
      reviewState: 'approved',
      submittedAt: new Date(),
      submittedBy: 'user',
      submissionType: 'approve',
    } as ReviewInteractionMessage);

    // ✅ 清除此layer的pause事件追踪，允许下一个layer的pause事件
    const layer = message.layer;
    processedPauseEventsRef.current.delete(layer);
    logger.chatPanel.info(`清除 Layer ${layer} pause事件追踪`, taskId);

    addMessage(createSystemMessage('✅ 已批准，继续执行下一层...'));
    setShowReviewPanel(false);
    setStatus('planning');

    if (response.resumed) {
      reconnectSSE?.();  // 重新创建 SSE 连接
    }
  } catch (error: any) {
    addMessage(createErrorMessage(`批准失败: ${error.message || '未知错误'}`));
  }
}, [taskId, addMessage, setStatus, setShowReviewPanel, reconnectSSE]);
```

**函数调用关系**:
```
用户点击批准按钮
  └─> handleReviewInteractionApprove()  [ChatPanel.tsx:363]
       ├─> planningApi.approveReview(taskId)  [lib/api.ts]
       │    └─> POST /api/planning/review/{taskId}  [后端 API]
       ├─> addMessage(reviewState: 'approved')  [Line 369-378]
       │    └─> UnifiedPlanningContext.addMessage()  [contexts/UnifiedPlanningContext.tsx]
       ├─> processedPauseEventsRef.current.delete(layer)  [Line 386]
       ├─> addMessage(systemMessage)  [Line 382]
       ├─> setShowReviewPanel(false)  [Line 389]
       ├─> setStatus('planning')  [Line 390]
       └─> reconnectSSE?.()  [Line 393]
            └─> useTaskSSE.reconnect()  [hooks/useTaskSSE.ts:150]
                 └─> 重置 initializingRef，创建新 SSE 连接
```

#### 4. 任务完成时清理所有追踪 (Lines 207-210)

```typescript
onComplete: (data: any) => {
  logger.chatPanel.info('任务完成', data, taskId);
  console.log('[ChatPanel] Task completed:', data);
  setStatus('completed');

  // ✅ 清除所有追踪sets
  completedLayersRef.current.clear();
  processedPauseEventsRef.current.clear();
  logger.chatPanel.info('清除所有layer和pause事件追踪', taskId);

  addMessage({
    type: 'result',
    content: `🎉 规划任务已完成！\n\n村庄：${villageFormData?.projectName || '村庄'}`,
    // ...
  } as any);
},
```

**完整的前端-后端函数调用关系图**:

```
┌─────────────────────────────────────────────────────────────────┐
│                         后端 (Backend)                          │
└─────────────────────────────────────────────────────────────────┘
  │
  ├─ POST /api/planning/start  [backend/api/planning.py:120]
  │   ├─> _generate_session_id()
  │   ├─> sessions[session_id] = {...}
  │   └─> BackgroundTasks.add_task(_execute_graph_in_background)
  │       └─> asyncio.create_task(_execute_graph_in_background(...))
  │           ├─> sent_pause_events = _sessions[session_id].setdefault("sent_pause_events", set())
  │           ├─> 检测 pause_after_step [Line 324]
  │           │   ├─> if pause_event_key not in sent_pause_events:
  │           │   │   ├─> events_list.append(pause_event)
  │           │   │   ├─> sent_pause_events.add(pause_event_key)
  │           │   │   └─> _stream_states[session_id] = "paused"
  │           │   └─> return (跳过重复)
  │           └─> yield format_sse("pause", {...})
  │
  ├─ GET /api/planning/stream/{session_id}  [Line 200]
  │   └─> SSE 流式传输
  │       ├─> progress 事件
  │       ├─> layer_completed 事件
  │       ├─> pause 事件 ⭐ (已去重)
  │       └─> stream_paused 事件
  │
  └─ POST /api/planning/review/{session_id}  [Line 670]
      ├─> review_action(request.action)
      │   ├─> if action == "approve":
      │   ├─> logger.info() 批准前状态日志 [Line 714-718]
      │   ├─> _stream_states[session_id] = "active"
      │   ├─> initial_state["pause_after_step"] = False
      │   ├─> 清除 sent_pause_events [Line 733]
      │   └─> _resume_graph_execution()
      │       └─> asyncio.create_task(_execute_graph_in_background(...))

┌─────────────────────────────────────────────────────────────────┐
│                         前端 (Frontend)                          │
└─────────────────────────────────────────────────────────────────┘
  │
  ├─ useTaskSSE(taskId, callbacks)  [hooks/useTaskSSE.ts]
  │   ├─> 创建 EventSource(GET /api/planning/stream/{session_id})
  │   ├─> 监听 'pause' 事件
  │   │   └─> onPause(data)  [ChatPanel.tsx:180]
  │   │       ├─> if processedPauseEventsRef.current.has(layer): return
  │   │       ├─> processedPauseEventsRef.current.add(layer)
  │   │       ├─> setStatus('paused')
  │   │       ├─> addMessage(review_interaction)
  │   │       └─> loadCheckpoints()
  │   └─> 监听 'stream_paused' 事件
  │       └─> eventSourceRef.current.close()
  │
  ├─ UnifiedPlanningContext  [contexts/UnifiedPlanningContext.tsx]
  │   ├─> addMessage(message)
  │   ├─> setStatus(status)
  │   └─> reconnectSSE()
  │
  └─ handleReviewInteractionApprove(message)  [ChatPanel.tsx:363]
      ├─> planningApi.approveReview(taskId)  [lib/api.ts]
      │   └─> POST /api/planning/review/{taskId}
      ├─> addMessage(reviewState: 'approved')
      ├─> processedPauseEventsRef.current.delete(layer) ⭐
      ├─> setShowReviewPanel(false)
      ├─> setStatus('planning')
      └─> reconnectSSE()
          └─> 创建新的 SSE 连接

┌─────────────────────────────────────────────────────────────────┐
│                      数据流向 (Data Flow)                        │
└─────────────────────────────────────────────────────────────────┘

1. 启动规划流程
   用户提交表单
     → VillageInputForm.onSubmit()
     → planningApi.startPlanning(data)
     → POST /api/planning/start
     → 后端创建 session，启动后台执行
     → 返回 { session_id, status: "pending" }
     → ChatPanel 使用 taskId 建立 SSE 连接

2. Layer 完成后暂停
   后台执行完成 Layer 1
     → 检测到 pause_after_step = true
     → 检查 sent_pause_events（去重）✅
     → 添加 pause 事件到队列
     → SSE 发送 pause 事件
     → useTaskSSE.onPause(data)
     → 检查 processedPauseEventsRef（去重）✅
     → 添加 review_interaction 消息
     → 显示审查面板

3. 批准后继续
   用户点击批准
     → handleReviewInteractionApprove(message)
     → planningApi.approveReview(taskId)
     → POST /api/planning/review/{taskId} {action: "approve"}
     → 后端清除 pause_after_step，清除 sent_pause_events ✅
     → _resume_graph_execution() 启动新的后台执行
     → 返回 { resumed: true }
     → 前端清除 processedPauseEventsRef.current.delete(layer) ✅
     → reconnectSSE() 创建新 SSE 连接
     → 继续执行 Layer 2

4. 任务完成
   后台执行完成所有 Layer
     → SSE 发送 completed 事件
     → useTaskSSE.onCompleted()
     → ChatPanel.onComplete(data)
     → 清除 completedLayersRef 和 processedPauseEventsRef ✅
     → 显示完成消息
```

**关键改进点**:

1. **双重去重防护** ✅
   - 后端：`sent_pause_events` Set 追踪已发送的 pause 事件
   - 前端：`processedPauseEventsRef` 追踪已处理的 pause 事件
   - 即使某一层失败，另一层也能提供保护

2. **状态一致性** ✅
   - 批准前后端状态同步
   - 清除 pause_after_step 标志
   - 清除 sent_pause_events 和 processedPauseEventsRef

3. **清理机制** ✅
   - 批准后：清除当前 layer 的 pause 追踪
   - 任务完成：清除所有追踪 (completedLayersRef + processedPauseEventsRef)

4. **详细日志** ✅
   - 后端：批准前状态日志，pause 事件去重日志
   - 前端：pause 事件处理日志，去重跳过日志

**修复效果**:
- ✅ 每个 Layer 只显示一个审查面板
- ✅ 点击批准立即成功，无错误
- ✅ 顺畅的 Layer 1 → 2 → 3 执行流程

---

## 函数调用关系图 ⭐ NEW

### 完整的函数调用链

#### 1. 启动规划流程

```
用户操作
  └─> VillageInputForm.handleFileUpload()
      ├─> FileReader.readAsText()
      └─> setUploadedFileContent(content)

  └─> VillageInputForm.handleSubmit()
      ├─> planningApi.startPlanning(data)
      │   └─> fetch POST /api/planning/start
      │       └─> backend/api/planning.py:start_planning() [Line 120]
      │           ├─> _generate_session_id() [Line 96]
      │           ├─> sessions[session_id] = {state, status, created_at} [Line 127]
      │           ├─> BackgroundTasks.add_task(_execute_graph_in_background) [Line 133]
      │           │   └─> asyncio.create_task(_execute_graph_in_background(...))
      │           │       ├─> create_village_planning_graph() [src/orchestration/main_graph.py]
      │           │       ├─> create_output_manager() [src/utils/output_manager.py]
      │           │       └─> _execute_graph_in_background() [backend/api/planning.py:200]
      │           └─> return { success, session_id, status, message }
      └─> router.push(`/village/${session_id}`)
```

#### 2. SSE 连接建立

```
ChatPanel 初始化
  └─> useEffect(() => {
        if (taskId) {
          const { isConnected, close, reconnect } = useTaskSSE(taskId, {
            onProgress: handleProgress,
            onLayerCompleted: handleLayerCompleted,
            onPause: handlePause,  // ⭐ Pause 事件处理
            onStreamPaused: handleStreamPaused,
            onError: handleError,
            onCompleted: handleComplete,
          })
        }
      }, [taskId])

  useTaskSSE() [hooks/useTaskSSE.ts]
    ├─> const eventSourceRef = useRef<EventSource>(null)
    ├─> const initializingRef = useRef(false)
    ├─> useEffect(() => {
          if (taskId && !initializingRef.current) {
            initializingRef.current = true
            const es = new EventSource(`${API_URL}/planning/stream/${taskId}`)
            eventSourceRef.current = es

            // 注册事件监听器
            es.addEventListener('pause', (e) => {
              const data = JSON.parse(e.data)
              onPause?.(data)  // ⭐ 调用 ChatPanel.onPause
            })

            es.addEventListener('stream_paused', (e) => {
              console.log('[SSE] Stream paused, closing connection')
              eventSourceRef.current?.close()
              onStreamPaused?.()
            })

            // ... 其他事件监听器
          }
        }, [taskId, callbacks])
    └─> return { isConnected, error, close, reconnect }
```

#### 3. Pause 事件处理（核心修复）

```
后端发送 pause 事件
  └─> _execute_graph_in_background() [backend/api/planning.py:200]
      ├─> 检测 event.get("pause_after_step") [Line 324]
      │   ├─> current_layer = event.get("current_layer", 1)
      │   ├─> pause_event_key = f"pause_layer_{current_layer}"
      │   ├─> sent_pause_events = _sessions[session_id].setdefault("sent_pause_events", set()) [Line 266]
      │   ├─> if pause_event_key not in sent_pause_events:  [Line 331]
      │   │   ├─> pause_event = {type: "pause", current_layer, ...} [Line 333]
      │   │   ├─> events_list.append(pause_event) [Line 339]
      │   │   ├─> sent_pause_events.add(pause_event_key) [Line 342]
      │   │   ├─> stream_paused_event = {...} [Line 344]
      │   │   ├─> events_list.append(stream_paused_event) [Line 349]
      │   │   ├─> _stream_states[session_id] = "paused" [Line 350]
      │   │   ├─> logger.info(f"✓ pause事件已添加到队列 (Layer {current_layer})") [Line 351]
      │   │   └─> return
      │   └─> else:  [Line 354]
      │       ├─> logger.info(f"⚠️ 跳过重复的pause事件 (Layer {current_layer})") [Line 357]
      │       └─> return
      └─> yield format_sse("pause", pause_event_data)  [Line ~800]

前端接收 pause 事件
  └─> EventSource.addEventListener('pause', (e) => {...})
      └─> onPause(data)  [ChatPanel.tsx:180]
          ├─> logger.chatPanel.info('执行暂停', data, taskId) [Line 181]
          ├─> layer = currentLayer || 1  [Line 184]
          ├─> if (processedPauseEventsRef.current.has(layer))  [Line 187]  ⭐ 去重检查
          │   ├─> console.log('Pause event already processed, skipping') [Line 188]
          │   ├─> logger.chatPanel.warn('跳过重复的 pause事件', ...) [Line 189]
          │   └─> return  [Line 190]  ⭐ 跳过重复
          ├─> processedPauseEventsRef.current.add(layer)  [Line 194]  ⭐ 标记已处理
          ├─> logger.chatPanel.info('标记 pause事件为已处理', ...) [Line 195]
          ├─> setStatus('paused')  [Line 197]
          ├─> addMessage({  [Line 198]
          │   type: 'review_interaction',
          │   layer: layer,
          │   content: '规划已暂停，请审查后决定下一步操作',
          │   reviewState: 'pending',
          │   ...otherProps
          │ } as ReviewInteractionMessage)
          │   └─> UnifiedPlanningContext.addMessage()  [contexts/UnifiedPlanningContext.tsx]
          │       └─> setMessages(prev => [...prev, message])
          └─> loadCheckpoints()  [Line 217]
              └─> planningApi.getTaskStatus(taskId)
                  └─> fetch GET /api/planning/status/{taskId}
```

#### 4. 批准流程（核心修复）

```
用户点击批准按钮
  └─> handleReviewInteractionApprove(message)  [ChatPanel.tsx:363]
      ├─> if (!taskId) return  [Line 364]
      ├─> try {  [Line 366]
      │   ├─> const response = await planningApi.approveReview(taskId)  [Line 368]
      │   │   └─> fetch POST /api/planning/review/{taskId}  [lib/api.ts]
      │   │       └─> body: JSON.stringify({action: 'approve'})
      │   │       └─> backend/api/planning.py:review_action() [Line 670]
      │   │           ├─> session = sessions.get(session_id)  [Line 676]
      │   │           ├─> initial_state = session["state"]  [Line 677]
      │   │           ├─> review_id = initial_state.get("review_id", "")  [Line 679]
      │   │           ├─> is_pause_mode = initial_state.get("pause_after_step", False)  [Line 680]
      │   │           ├─> is_review_mode = initial_state.get("waiting_for_review", False)  [Line 681]
      │   │           ├─> if request.action == "approve":  [Line 711]
      │   │           │   ├─> logger.info('批准请求 - 状态检查')  [Line 714]
      │   │           │   ├─> logger.info(f'  - review_id: {review_id}')  [Line 715]
      │   │           │   ├─> logger.info(f'  - pause_after_step: {is_pause_mode}')  [Line 716]
      │   │           │   ├─> logger.info(f'  - waiting_for_review: {is_review_mode}')  [Line 717]
      │   │           │   ├─> logger.info(f'  - current_layer: {initial_state.get("current_layer", 1)}')  [Line 718]
      │   │           │   ├─> _stream_states[session_id] = "active"  [Line 721]
      │   │           │   ├─> if session_id in _active_executions:  [Line 724]
      │   │           │   │   └─> logger.warning('检测到正在运行的后台执行')  [Line 725]
      │   │           │   ├─> initial_state["waiting_for_review"] = False  [Line 728]
      │   │           │   ├─> initial_state["pause_after_step"] = False  [Line 729]
      │   │           │   ├─> initial_state["human_feedback"] = ""  [Line 730]
      │   │           │   ├─> initial_state["__interrupt__"] = False  [Line 731]
      │   │           │   ├─> session["status"] = TaskStatus.running  [Line 732]
      │   │           │   ├─> if "sent_pause_events" in _sessions[session_id]:  [Line 735]
      │   │           │   │   └─> logger.info('清除pause事件追踪')  [Line 736]
      │   │           │   └─> return await _resume_graph_execution(session_id, initial_state)  [Line 748]
      │   │           │       └─> checkpointer = _session_checkpointer[session_id]  [Line 437]
      │   │           │       └─> asyncio.create_task(_execute_graph_in_background(...))  [Line 446]
      │   │           └─> return { success: true, message: "已批准，继续执行", resumed: true }
      │   │       └─> return response.json()
      │   │
      │   ├─> addMessage({  [Line 370]
      │   │   ...message,
      │   │   reviewState: 'approved',
      │   │   submittedAt: new Date(),
      │   │   submittedBy: 'user',
      │   │   submissionType: 'approve',
      │   │ } as ReviewInteractionMessage)
      │   │   └─> UnifiedPlanningContext.addMessage()
      │   │
      │   ├─> const layer = message.layer  [Line 385]
      │   ├─> processedPauseEventsRef.current.delete(layer)  [Line 386]  ⭐ 清除追踪
      │   ├─> logger.chatPanel.info('清除 pause事件追踪', ...) [Line 387]
      │   ├─> addMessage(createSystemMessage('✅ 已批准，继续执行下一层...'))  [Line 389]
      │   ├─> setShowReviewPanel(false)  [Line 390]
      │   ├─> setStatus('planning')  [Line 391]
      │   │
      │   └─> if (response.resumed) {  [Line 393]
      │       └─> reconnectSSE?.()  [Line 395]
      │           └─> useTaskSSE.reconnect()  [hooks/useTaskSSE.ts:150]
      │               ├─> if (eventSourceRef.current) {  [Line 152]
      │               │   └─> eventSourceRef.current.close()  [Line 153]
      │               ├─> initializingRef.current = false  [Line 155]
      │               ├─> setIsConnected(false)  [Line 156]
      │               └─> useEffect 会重新创建 EventSource
      │
      └─> catch (error: any) {  [Line 399]
          └─> addMessage(createErrorMessage(`批准失败: ${error.message}`))  [Line 400]
```

#### 5. 任务完成清理

```
后端所有 Layer 完成
  └─> _execute_graph_in_background()  [backend/api/planning.py]
      └─> yield format_sse("completed", final_output)

前端接收 completed 事件
  └─> EventSource.addEventListener('completed', (e) => {...})
      └─> onCompleted(data)  [ChatPanel.tsx:203]
          ├─> logger.chatPanel.info('任务完成', data, taskId) [Line 204]
          ├─> console.log('[ChatPanel] Task completed:', data) [Line 205]
          ├─> setStatus('completed') [Line 206]
          ├─> completedLayersRef.current.clear()  [Line 208]  ⭐ 清除所有追踪
          ├─> processedPauseEventsRef.current.clear()  [Line 209]  ⭐ 清除所有追踪
          ├─> logger.chatPanel.info('清除所有layer和pause事件追踪', taskId) [Line 210]
          └─> addMessage({  [Line 211]
              type: 'result',
              content: `🎉 规划任务已完成！\n\n村庄：${villageFormData?.projectName || '村庄'}`,
              villageName: villageFormData?.projectName || '村庄',
              sessionId: taskId || '',
              layers: ['Layer 1', 'Layer 2', 'Layer 3'],
              resultUrl: `/village/${taskId}`,
            } as any)
```

### 关键依赖关系总结

```typescript
// 前端核心文件依赖
frontend/src/components/chat/ChatPanel.tsx
  ├─> types/message.ts (Message, ReviewInteractionMessage)
  ├─> types/message-types.ts (具体消息类型定义)
  ├─> types/message-guards.ts (isReviewInteractionMessage)
  ├─> contexts/UnifiedPlanningContext.tsx
  │   ├─> React.createContext()
  │   ├─> usePlanningContext()
  │   └─> 状态: { messages, status, taskId, ... }
  ├─> hooks/useTaskSSE.ts
  │   ├─> EventSource API
  │   ├─> useRef, useEffect, useCallback
  │   └─> 返回: { isConnected, error, close, reconnect }
  ├─> lib/api.ts
  │   ├─> planningApi.approveReview()
  │   ├─> planningApi.getTaskStatus()
  │   └─> fetch API
  └─> lib/logger.ts
      └─> logger.chatPanel.info/warn/error

// 后端核心文件依赖
backend/api/planning.py
  ├─> src/orchestration/main_graph.py
  │   └─> create_village_planning_graph()
  ├─> src/utils/output_manager.py
  │   └─> create_output_manager()
  ├─> backend/services/rate_limiter.py
  │   └─> rate_limiter.is_allowed()
  ├─> backend/utils/logging.py
  │   └─> get_logger(__name__)
  ├─> backend/schemas.py
  │   ├─> TaskStatus
  │   └─> 各种 Pydantic 模型
  └─> 全局变量
      ├─> _sessions: Dict[str, Dict]  # 会话存储
      ├─> _active_executions: Dict[str, bool]  # 执行追踪
      ├─> _session_checkpointer: Dict[str, Any]  # 检查点存储
      └─> _stream_states: Dict[str, str]  # 流状态追踪

// 数据流向
前端 → 后端: fetch API (POST/GET)
后端 → 前端: SSE (Server-Sent Events)
状态同步: UnifiedPlanningContext (前端) + sessions (后端)
```

---

## 参考资源

- [Next.js 文档](https://nextjs.org/docs)
- [React 文档](https://react.dev/)
- [TypeScript 文档](https://www.typescriptlang.org/docs/)
- [Tailwind CSS 文档](https://tailwindcss.com/docs)
- [项目组件架构文档](../FRONTEND_COMPONENT_ARCHITECTURE.md)
- [前端视觉设计指南](../FRONTEND_VISUAL_GUIDE.md)
